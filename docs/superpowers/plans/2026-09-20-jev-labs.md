# Jev Labs Implementation Plan

**Goal:** Give every upstream playground example an interactive Lab and reproduce the component catalog's navigation and reuse workflow.

**Architecture:** A pinned, attributed upstream catalog feeds a typed Lab registry. Shared Jev response primitives live in packages/ui. App-specific previews render editable inputs, local typed fixtures, and domain-specific evidence. Server-only source loading supplies actual source files and honest workspace setup/import snippets. Labs use stable hash links and a searchable grouped sidebar with a narrow-screen drawer.

**Stack:** Existing Next.js, React, Base UI, shiki, TypeScript, Playwright; no dependency or provider changes.

- [x] Inventory and pin upstream catalog + named workspace scenarios; save coverage manifest and license.
- [x] Add failing browser checks for Lab navigation, sources, and typed examples.
- [x] Add reusable `jev-result` and `jev-contract` components, registry entries, and demos.
- [x] Add typed catalog definitions and local response-design preview with editing, exact A/B variants, empty/error states, and reference notes.
- [x] Add domain previews for workspace scenarios (routing/policy, source evidence, ranking, games/actions, solving, rebuilds).
- [x] Replace scene carousel with grouped sidebar, mobile browser, deep links, source/setup/import tabs, and search integration. Retain existing scenes.
- [x] Exercise every upstream example, source/import/preview tabs, and significant state transitions. Verify responsive layout, themes, RTL, keyboard and reduced motion.
- [x] Run root lint/typecheck/build and Playwright; record coverage by pinned upstream identifier.

Do not commit automatically. Preserve the previous uncommitted optical-studio work and avoid writes to the sibling playground.
